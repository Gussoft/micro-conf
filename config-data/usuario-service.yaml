server:
  port: 9091
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:9094/eureka
    instance:
      hostname: localhost