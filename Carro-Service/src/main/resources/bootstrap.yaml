spring:
  application:
    name: carro-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:9090
  datasource:
    url: jdbc:mysql://localhost:3306/micro?serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  banner:
    location: guss.txt


